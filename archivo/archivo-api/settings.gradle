rootProject.name = 'archivo-api'

include ':common'
include ':archivo:archivo-core'

project(':common').projectDir = new File('../../common')
project(':archivo:archivo-core').projectDir = new File('../archivo-core')
